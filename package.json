{
  "name": "bcalgorix-contest",
  "version": "1.0.0",
  "description": "BCAlgorix - Blind Typing Contest Web Application",
  "scripts": {
    "start": "node server/src/server.js",
    "server": "node --watch server/src/server.js",
    "client": "npm --prefix client run dev",
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build",
    "install:all": "npm --prefix server install && npm --prefix client install"
  },
  "keywords": [
    "blind-typing",
    "contest",
    "bcalgorix",
    "react",
    "mongodb"
  ],
  "author": "BCAlgorix Fest Team",
  "license": "ISC"
}
